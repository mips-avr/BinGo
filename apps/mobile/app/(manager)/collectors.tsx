import { DataCard, DataListView } from '../../src/components/pivot/DataListView'; import { useManagerOperations } from '../../src/features/pivot/hooks'; export default function Screen(){ const q=useManagerOperations(); return <DataListView title="Petugas dan Kartu" subtitle="Kelola pekerja formal, penugasan, dan kartu NFC." query={q} renderItems={(d)=>d.collectors.map((x:any)=><DataCard key={x.id} title={x.user.name} detail={x.employeeNo} meta={`${x.cards.length} kartu aktif`} />)} />; }
