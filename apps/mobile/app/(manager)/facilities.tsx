import { DataCard, DataListView } from '../../src/components/pivot/DataListView'; import { useFacilities } from '../../src/features/pivot/hooks'; export default function Screen(){ const q=useFacilities(); return <DataListView title="Fasilitas" subtitle="Direktori fasilitas dan tanggal verifikasinya." query={q} renderItems={(d)=>d.map((x:any)=><DataCard key={x.id} title={x.name} detail={x.address} meta={`Diverifikasi ${new Date(x.verifiedAt).toLocaleDateString('id-ID')}`} />)} />; }
