import { DataCard, DataListView } from '../../src/components/pivot/DataListView'; import { useManagerOperations } from '../../src/features/pivot/hooks'; export default function Screen(){ const q=useManagerOperations(); return <DataListView title="Wilayah dan Pelanggan" subtitle="Kepadatan wilayah menentukan efisiensi layanan kolektif." query={q} renderItems={(d)=>d.areas.map((x:any)=><DataCard key={x.id} title={x.name} detail={`${x._count.households} rumah tangga`} meta={x.status.replaceAll('_',' ')} />)} />; }
